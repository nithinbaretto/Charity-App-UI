class HomeModel {
  final String photoUrl;
  final String title;
  final String description;
  final double moneyRaised;
  final double goal;
  final int likes;

  HomeModel(this.photoUrl, this.title, this.description, this.moneyRaised,
      this.goal, this.likes);
}
